---
import { marked } from '@utils/marked.js';

const {content, class:classList, reveal = false} = Astro.props
---

 <div
    class=`paragraphe ${classList}` set:html={marked(content)} data-reveal-lines={reveal ? '' : undefined}
  />