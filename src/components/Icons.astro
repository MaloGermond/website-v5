---
const { name, size = '24', class:classList="" } = Astro.props;
---
<svg width={size} height={size} viewBox="0 0 32 32" fill="none" stroke="currentColor" class={"dark:text-gray-50 "+ classList}>
  <use href={`./src/assets/icons/${name}.svg`} />
</svg>