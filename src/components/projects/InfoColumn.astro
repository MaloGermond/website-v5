---
const { title, items } = Astro.props;
---

<div class="p-4">
  <h3 class="text-xl text-gray-400 dark:text-gray-400">
    {title}
  </h3>{
    Object(items).map((el) => (
      <p class="text-xl dark:text-gray-50">{el}</p>
    ))
  }
</div>
